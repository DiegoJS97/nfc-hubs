# Dependencias
node_modules/

# Variables de entorno y secretos
.env
.env.local
.env.*.local
.dev.vars

# Salida de build de Eleventy
_site/
.cache/

# Cloudflare
.wrangler/

# Resultados de tests (Playwright)
test-results/
playwright-report/

# Claude Code: ajustes locales de cada persona.
# .claude/settings.json SI se versiona (reglas compartidas del repo).
.claude/settings.local.json

# Logs
*.log
npm-debug.log*

# Sistema operativo
.DS_Store
Thumbs.db

# Editor (opcional, descomenta si no quieres compartir tu config de VS Code)
# .vscode/
