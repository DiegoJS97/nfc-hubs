---
layout: layouts/hub.njk
slug: copas
permalink: /copas/index.html
---

{#
  Binding only. This file names which business to render and where the page lives; every
  label, destination, and value comes from business.json via the data cascade.

  That is what makes SC-004 true: confirming real data never touches this file, the layout,
  or any partial. The permalink is the tag-encoded URL segment (contracts/hub-url.md) and
  must not change once tags are written.
#}
